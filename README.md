# Wormhole

Wormhole is ngrok for agents and worktrees: a fast, secure CLI that exposes local HTTP(S) and TCP services through a self-hosted relay, Tailscale, Cloudflare, or several endpoints at once.

## Installation

Installation packages and instructions will be added with the first release.

## Development

The staged implementation plan lives in [`docs/plan`](docs/plan/00-overview.md).
